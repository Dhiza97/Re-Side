<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Admin Sign In</title>
    <link rel="stylesheet" href="/css/style.css"> <!-- Update this with your actual stylesheet path -->
</head>
<body>
    <div class="admin-container">
        
        <% if (errorMessage) { %>
            <div class="alert alert-danger">
                <%= errorMessage %>
            </div>
        <% } %>

        <div class="admin-form-items">
            <h1>Admin Sign In</h1>

            <div class="admin-signin-form">
                <form action="/admin/signin" method="POST">
                    <div class="form-group">
                        <input type="email" id="email" name="email" class="form-control" required>
                        <span></span>
                        <label for="email">Email:</label>
                    </div>
        
                    <div class="form-group">
                        <input type="password" id="password" name="password" class="form-control" required>
                        <span></span>
                        <label for="password">Password:</label>
                    </div>
        
                    <button type="submit" class="btn btn-primary">Sign In</button>
                </form>
            </div>
        </div>
        
    </div>

    <script src="/path/to/your/scripts.js"></script> <!-- Update this with your actual script path -->
</body>
</html>